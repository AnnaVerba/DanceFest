// What one dancer owes for one entry — their own part of the number
// (see EntryCharge.shareOf), so a cabinet can total by dancer.
export interface ParticipantAmount {
  participantId: string;
  amount: number;
}
