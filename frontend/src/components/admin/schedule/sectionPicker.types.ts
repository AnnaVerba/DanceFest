// The wording of one use of SectionPickerModal.
export interface SectionPickerTexts {
  title: string;
  submitLabel: string;
  submittingLabel: string;
  // Optional note under the pickers.
  hint?: string;
}
