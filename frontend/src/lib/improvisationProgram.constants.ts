// Improvisation has no duration limit of its own: its stage time comes from
// the competition's timing rules, so a program with this name stem is never
// given a limit in a special category.
export const IMPROVISATION_PROGRAM_NAME_STEM = 'імпровіз';
