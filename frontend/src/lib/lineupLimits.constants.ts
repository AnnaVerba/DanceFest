// Lineups that carry their own on-stage limit in the timings settings;
// the labels match the ones the backend resolves from the dancer count.
export const LINEUP_LIMIT_LABELS = ['Дуо', 'Тріо', 'Група'] as const;
