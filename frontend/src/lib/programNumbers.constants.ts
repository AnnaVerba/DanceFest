// The two numbering systems a programme carries: nomination blocks and the
// performers inside them. The technical programme heads its columns with these
// marks, both programmes put them in front of the numbers themselves — one
// source, so the views cannot drift into calling the same number by two names.
export const NOMINATION_NUMBER_MARK = 'F';
export const PARTICIPANT_NUMBER_MARK = '№';
