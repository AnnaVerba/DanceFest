// One organizer in the picker: the name shown on the chip and the account
// behind it (NO_ACCOUNT_ORGANIZER_ID for a name that has no account).
export interface OrganizerChip {
  id: string;
  name: string;
}
